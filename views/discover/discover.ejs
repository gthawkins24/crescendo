<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>

    <main>
        <% if (circs.length > 0) { %>
            <div class="container">
                <% for (let circle of circs) { %>
                <div class="col-lg-12">
                    <div class="card" style="width: 35rem;">
                        <img src="<%= circle.imageUrl %>" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title"><%= circle.title %></h5>
                            <p class="card-text"><%= circle.description %></p>
                            <a href="#" class="btn btn-primary">THIS WILL LINK TO THIS CIRCLE</a>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>
        <% } %>
    </main>

    <%- include('../includes/end.ejs') %>